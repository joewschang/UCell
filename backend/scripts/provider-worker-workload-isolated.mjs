import assert from'node:assert/strict';import{randomUUID}from'node:crypto';import{createRequire}from'node:module';import{fileURLToPath}from'node:url';import{spawnSync}from'node:child_process';const require=createRequire(new URL('../packages/database/package.json',import.meta.url)),{PrismaClient}=require('@prisma/client'),base=new URL(process.env.DATABASE_URL??'postgresql://ucell:ucell_dev@127.0.0.1:5432/ucell');assert.ok(['localhost','127.0.0.1'].includes(base.hostname));const database='ucell_provider_workload_'+randomUUID().replaceAll('-',''),control=new URL(base);control.pathname='/postgres';const target=new URL(base);target.pathname='/'+database;const admin=new PrismaClient({datasources:{db:{url:control.href}}}),cwd=fileURLToPath(new URL('../',import.meta.url)),env={...process.env,DATABASE_URL:target.href};function run(args){const r=spawnSync(process.execPath,args,{cwd,env,stdio:'inherit'});if(r.error)throw r.error;assert.equal(r.status,0,'Provider workload child failed');}let created=false;try{await admin.$executeRawUnsafe('CREATE DATABASE "'+database+'"');created=true;run([require.resolve('prisma/build/index.js'),'migrate','deploy','--schema','packages/database/prisma/schema.prisma']);run(['scripts/provider-worker-workload-test.mjs']);console.log('PROVIDER_WORKLOAD_ISOLATED_PASS');}finally{if(created){await admin.$executeRawUnsafe('DROP DATABASE "'+database+'" WITH (FORCE)');console.log('PROVIDER_WORKLOAD_ISOLATED_CLEANUP_PASS');}await admin.$disconnect();}