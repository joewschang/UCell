import {QualificationPlacementService} from '../src/modules/qualification/qualification-placement.service';
describe('pending placement member-safe read model',()=>{it('returns memberNo and Ball numbers without internal UUIDs',async()=>{const db:any={qualification:{findMany:jest.fn().mockResolvedValueOnce([{qualificationId:'sponsor-q'}]).mockResolvedValueOnce([{qualificationId:'pending-q',ballNo:'A000005'},{qualificationId:'sponsor-q',ballNo:'A000004'}])},qualificationSetup:{findMany:jest.fn().mockResolvedValue([{qualificationId:'pending-q',ownerPersonId:'owner-p',finalSponsorQualificationId:'sponsor-q',packageType:'STARTER',setupStatus:'PLACEMENT_PENDING',placementRequestedAt:new Date('2026-01-01T00:00:00Z'),placementDueAt:new Date('2026-01-04T00:00:00Z'),placedAt:null,setupPolicyVersion:'V1'}])},person:{findMany:jest.fn().mockResolvedValue([{personId:'owner-p',memberNo:'2609000001'}])}};const service=new QualificationPlacementService(db,{} as any,{} as any,{} as any);const rows=await service.pendingForSponsorOwner('owner-p',new Date('2026-01-02T00:00:00Z'));expect(rows[0]).toMatchObject({pendingBallNo:'A000005',pendingMemberNo:'2609000001',sponsorBallNo:'A000004',packageType:'STARTER',paymentState:'CONFIRMED'});expect(rows[0]).not.toHaveProperty('qualificationId');expect(rows[0]).not.toHaveProperty('ownerPersonId');expect(rows[0]).not.toHaveProperty('sponsorQualificationId');});it('rejects a UUID before it can be used as a public Ball number',async()=>{const findUnique=jest.fn(),service=new QualificationPlacementService({qualification:{findUnique}} as any,{} as any,{} as any,{} as any);await expect(service.placeBySponsorOwnerBallNo('person',{ballNo:'11111111-1111-4111-8111-111111111111',binaryParentBallNo:'A000004',side:'LEFT'},'key','request')).rejects.toMatchObject({response:{code:'PLACEMENT_BALL_NO_INVALID'}});expect(findUnique).not.toHaveBeenCalled();});});
