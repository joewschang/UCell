import {PaperReceiptService} from '../src/modules/order/paper-receipt.service';
describe('PaperReceiptService',()=>{const base:any={receiptReference:'R-1',paymentChannel:'BANK',paymentNamespace:'UCELL',actorId:'approver',key:'k',requestId:'r'};it('requires dual control for qualification orders',async()=>{const db:any={paperReceiptEvidence:{findUnique:jest.fn().mockResolvedValue({paperReceiptEvidenceId:'x',orderId:'o',enteredBy:'approver',amount:{toString:()=> '100'},receivedAt:new Date(),paymentChannel:'BANK',receiptReference:'R-1'})},order:{findUnique:jest.fn().mockResolvedValue({purpose:'ENTRY'})}};await expect(new PaperReceiptService(db,{} as any).approve(base)).rejects.toMatchObject({response:{code:'PAPER_PAYMENT_DUAL_CONTROL_REQUIRED'}});});it('rejects already confirmed receipt',async()=>{const db:any={paperReceiptEvidence:{findUnique:jest.fn().mockResolvedValue({orderId:'o',enteredBy:'other',confirmedBy:'done'})},order:{findUnique:jest.fn().mockResolvedValue({purpose:'RETAIL'})}};await expect(new PaperReceiptService(db,{} as any).approve(base)).rejects.toMatchObject({response:{code:'PAPER_RECEIPT_ALREADY_CONFIRMED'}});});});
