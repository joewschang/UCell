import {PrismaClient} from '@prisma/client';import {createEffectiveMemberSponsorFixture} from './helpers/member-sponsor.fixture';
const url=process.env.PHASE2_TEST_DATABASE_URL;const describeDb=url?describe:describe.skip;describeDb('member sponsor fixture',()=>{let db:PrismaClient;beforeAll(()=>db=new PrismaClient({datasources:{db:{url}}}));afterAll(()=>db.$disconnect());it('creates an effective attached member ball',async()=>{await expect(db.$transaction(async tx=>{const f=await createEffectiveMemberSponsorFixture(tx as any);expect(f.qualification.ballNo).toMatch(/\d{6}$/);expect(await tx.binaryTreeMembership.findUnique({where:{qualificationId:f.qualification.qualificationId}})).not.toBeNull();throw new Error('ROLLBACK');})).rejects.toThrow('ROLLBACK');});});
