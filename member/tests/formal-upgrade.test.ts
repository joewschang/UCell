import {afterEach,beforeEach,expect,it,vi} from 'vitest';
import {consentFormalContract,getFormalRequiredContracts,saveFormalDraft} from '../src/memberData';
const envelope=(data:unknown,status=200)=>new Response(JSON.stringify({data,meta:{request_id:'formal-test',api_version:'v1',timestamp:'2026-09-17T00:00:00Z'}}),{status});
beforeEach(()=>vi.stubGlobal('sessionStorage',{getItem:()=>null}));afterEach(()=>{vi.unstubAllGlobals();vi.restoreAllMocks();});
it('reads formal contract, records explicit consent and sends sensitive draft only to authenticated API body',async()=>{const contract={id:'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',type:'FORMAL_MEMBERSHIP',version:'V1',title:'Formal',content:'Terms',contentHash:'a'.repeat(64),required:true,effectiveFrom:'2026-01-01T00:00:00Z',effectiveTo:null,acceptedAt:null},fetch=vi.fn().mockResolvedValueOnce(envelope([contract])).mockResolvedValueOnce(envelope({contractVersionId:contract.id,acceptedAt:'2026-09-17T00:00:00Z'},201)).mockResolvedValueOnce(envelope({id:'bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb',status:'DRAFT',version:1,nationalIdMasked:'***6789',bankAccountMasked:'***9012',replayed:false},201));vi.stubGlobal('fetch',fetch);expect((await getFormalRequiredContracts(new AbortController().signal))[0].id).toBe(contract.id);await consentFormalContract(contract.id,'11111111-1111-4111-8111-111111111111');const input={formalContractVersionId:contract.id,legalName:'王測試',gender:'FEMALE',birthDate:'1990-01-02',nationalId:'A123456789',communicationAddress:'TEST ADDRESS',phone:'+886912345678',email:'formal@example.invalid',bankCode:'012',bankAccount:'123456789012',accountHolder:'王測試'},saved=await saveFormalDraft(input,'22222222-2222-4222-8222-222222222222');expect(saved.status).toBe('DRAFT');expect(JSON.parse(fetch.mock.calls[2][1].body)).toEqual(input);expect(String(fetch.mock.calls[2][0])).not.toContain(input.nationalId);expect(String(fetch.mock.calls[2][0])).not.toContain(input.bankAccount);});