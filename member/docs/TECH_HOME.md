# Member home — Luminous Midnight

The authenticated home now uses midnight blue, ice-cyan highlights and restrained
violet accents. A luminous membership card, concentric decorative lines, clear
three-column metrics and outlined service icons replace the warm botanical home.
Decoration does not encode activity, biological results or business performance.

The theme is scoped to pathname `/` through `.tech-home`. Existing member pages
retain the previous shared theme. No API, auth, qualification, settlement or
commerce behavior changes. Missing values and pending awards remain explicit;
the demo banner is retained. No external font/image requests or invented product
photos. Motion is limited to existing reduced-motion-aware hover transitions.

Browser evidence includes desktop, mobile viewport and full-page screenshots.
Screenshots use synthetic fixtures and are not evidence of live LINE integration.
