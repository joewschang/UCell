# SA Phase 3 decision and traceability

Authority: SA decision supplied in this task; SSOT priority and R1.0B FROZEN remain unchanged.

| Decision | Implementation | Evidence |
|---|---|---|
| First accounting/business timezone is Asia/Taipei; timestamps remain UTC | Migration `20260915160000_sa_phase3_business_timezone` creates versioned accounting/EPV timezone parameters effective 2026-09-15 local midnight. No old parameter or sealed snapshot is updated. | Default/test DB migration logs; isolated DB migration replay |
| EPV month uses business timezone rather than UTC month | Existing `EpvMonthService.bounds` uses the captured timezone in PostgreSQL; DB tests straddle Taipei midnight at UTC 16:00. Future UTC in the fixture is TEST_ONLY, not an operational decision. | `default-db-golden.txt`, repeat run |
| RPV uses original recognition Qualification/Binary/Active/Parameter/period/recipient evidence | `sealRpvEvent` captures recognition month, timezone, UTC period bounds and original graph/award recipients. `verifyReplayEnvelope` rejects missing or inconsistent evidence and `replayRpvCancellation` verifies the original ledger event. | `db-regression.txt`; original Binary parent changed after capture; original recipient still receives adjustment |
| Missing historical evidence fails closed | No live tree/current recipient reconstruction. Old envelopes missing required period/recipient/parameter evidence cannot be silently upgraded. | Missing period, event, Binary and timezone DB cases; no ledger/posting success on missing evidence |
| All monetary mutation is append-only | Existing ledger/award/PAID rows remain unchanged; replay postings/recovery deltas are appended. No official monetary result is produced by this task; calculations run through application code in isolated test fixtures. | Original award/PAID invariants, DB append-only guards, replay regression |
| Person owns independent Qualifications; Sponsor and Binary remain separate (existing task baseline, no new SA decision) | Membership approval creates qualification histories and two separate relationship records in the same idempotent transaction. Sponsor A may differ from Binary parent B for an eligible placement. | 48 isolated DB assertions: separate tree records, first/third-left restriction, duplicate approval, injected audit failure after all writes rolls back all seven tables and application/idempotency, successful retry preserves sequence |
| Return/downstream replay does not rewrite original historical monetary records (existing SA append-only decision) | Production replay remains unchanged. v060/v062 regression checks all original Binary/Matching settlement batches and original carry/award rows after cumulative returns; cancellation retains its schedule row and appends RPV reversal. A later reversal contributes to the original event period and carry continues from the prior replay result. | 101 DB regression assertions; focused v060/v062 Jest cases use private evidence files, preserving CLI canonical output |
| Idempotent append-only lifecycle processing (existing task engineering requirement) | API and worker use shared `matureBonusAward` with row lock and atomic latest-state check/append. No pending period or monetary Award changes. | Checkpoint `930d0e7` reproduced two parallel EFFECTIVE events instead of one; fixed DB regression expects one, duplicate no-op and immutable original Award |
| EPV recipients follow Sponsor and historical Active evidence (existing SSOT/SA rule) | Test-only fixtures use the real recognition service, archived Parameter/Qualification/Active evidence and separate Sponsor/Binary paths. Consumption scope and event mapping remain unapproved. | Original Active self 840, five Active Sponsor generations 100.8 each, inactive Sponsor zero and Binary-only ancestor no award; existing return regression preserves historical Sponsor despite current-state changes |

Order profile engineering traceability (existing versioned/traceable rule and deterministic application calculation requirements, no new SA decision): checkpoint `17bc149` precedes the real OrderService/IdempotencyService profile test and missing-profile rejection test. TEST_ONLY persistence mocks verify server-side price/profile selection, effective period lookup, sealed per-line metadata, duplicate retaining original snapshot, and no order/payment/outbox/audit writes without a profile. This does not approve fixture rates/eligibility as production rules, formal PV/BV mapping or consumption scope, and does not claim DB concurrency/rollback evidence.

Vertical slice engineering traceability (existing idempotency and Transactional Outbox requirements, no new SA decision): checkpoint `a6fd20a` precedes three executable Person/payment/outbox cases. Actual services use stateful persistence mocks; assertions check duplicate/conflicting commands, one payment, PAID rejection of a second key, exact sealed SALE_CONFIRMED payload/correlation and the transaction-local audit delegate. This is sequential service evidence, not concurrent DB payment or rollback verification. Formal PV/BV mapping and consumption scope are not inferred.

DB engineering traceability (existing append-only and schema consistency requirements): checkpoint `bed3413` precedes actual DB trigger rejection/entire-row retention assertions and generated Prisma field mapping versus DB columns for five adjustment/workflow/replay models. No schema/rule modification or new monetary result is introduced. The cancellation-reference TODO naming migration 0005 is retained as Legacy Test Drift: current 0005 defers that table to canonical subscription schema in 0006. No TODO is deleted or counted as completed from this migration comment alone.

Replay delta traceability (existing SA deterministic cumulative return/append-only decision): checkpoint `1c03647` precedes six actual DB assertions linking positive historical deltas to newly appended compensating awards and negative deltas to original-award recovery records, preserving original award contents and historical metadata. Duplicate return processing preserves posting/award/recovery counts. Three corresponding v060 TODOs become executable; fixture amounts/rates do not establish production PV/BV mapping or eligible scope.

Direct Qualification traceability (existing independent Qualification, separate trees and permanent sequence requirements): checkpoint `57ca3c9` precedes real-service tests and ten fresh Golden DB assertions for first RIGHT denial, idempotent direct creation, separate Sponsor/Binary parent and preserved sequence across explicit TEST_ONLY interval closure. This is not an approval of operational exit workflow or concurrent direct-create coverage. No business rule or monetary implementation changes.

RPV recognition traceability (existing original recognition evidence and idempotency requirements): checkpoint `bce1d46` precedes two executable TODO conversions and five real DB assertions through RpvService. Original ledger Qualification/source/dueAt and schedule event/month identity are checked; sequential duplicate leaves exactly one original ledger event and retains all original awards plus the entire historical snapshot. No concurrent recognition or approved operational calendar/eligible scope claim is made.

Pending Decision: eligible-consumption scope, formal PV/BV mapping, production operational calendar/cut-off. No proportional RPV partial-refund allocation across months is inferred without explicit original allocation facts. Existing subscription UTC scheduling is an engineering/calendar issue to address without choosing an unapproved operational calendar.

Test governance correction: commits `f70c47b` through `10aa33e` are preserved as audit evidence. Unverified conversions were restored from `89bfed6`. In particular, constant self-comparisons and assertions against unrelated EPV/ledger evidence do not count as implementation coverage. Every original unresolved case is back in the executable TODO inventory.
